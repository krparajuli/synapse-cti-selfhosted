apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: jsonstor
  labels:
    app.kubernetes.io/name: jsonstor
    app.kubernetes.io/component: json-storage
spec:
  serviceName: jsonstor-headless
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: jsonstor
  template:
    metadata:
      labels:
        app.kubernetes.io/name: jsonstor
        app.kubernetes.io/component: json-storage
    spec:
      securityContext:
        runAsUser: 999
        runAsGroup: 999
        fsGroup: 999
      # Wait for AHA to be ready before starting JSONStor
      initContainers:
        - name: wait-for-aha
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "Waiting for AHA to be ready..."
              until nc -z aha.synapse.svc.cluster.local 27492; do
                echo "AHA not ready, retrying in 5s..."
                sleep 5
              done
              echo "AHA is ready."
      containers:
        - name: jsonstor
          image: vertexproject/synapse-jsonstor
          ports:
            - name: cell
              containerPort: 27492
              protocol: TCP
          envFrom:
            - configMapRef:
                name: jsonstor-config
            - secretRef:
                name: jsonstor-provision
                optional: true
          volumeMounts:
            - name: storage
              mountPath: /vertex/storage
          readinessProbe:
            tcpSocket:
              port: cell
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: cell
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "1Gi"
              cpu: "500m"
  volumeClaimTemplates:
    - metadata:
        name: storage
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 10Gi
