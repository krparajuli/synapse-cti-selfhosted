apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: jsonstor
spec:
  template:
    spec:
      containers:
        - name: jsonstor
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "2Gi"
              cpu: "1000m"
  volumeClaimTemplates:
    - metadata:
        name: storage
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 50Gi
